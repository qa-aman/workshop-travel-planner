# Decisions

One row per decision that shaped the product or the build. Newest at the top. A decision is anything where a reasonable engineer could have gone the other way. Record it here in the same turn it is made, before the next task starts.

Format for each entry: what was decided, the options considered, why this one, what it costs, and where it is applied. If a decision changes the spec, also log it in `docs/superpowers/specs/changelog.md` and edit the spec.

D-017 and D-018 were never assigned, the numbering skips them.

| ID | Date | Decision | Options considered | Why | Cost accepted | Applied in |
|---|---|---|---|---|---|---|
| D-031 | 20-09-2026 | Independent QA pass on D-030 found two more real gaps: `web/components/ItineraryView.tsx` hardcoded the literal string "JPY" in the FX caption regardless of destination, because `itinerary.schema.json`'s `fx` object never carried a currency code at all, so the web UI had no way to render anything else. Second, `review.md`'s rewritten like-mapping said "temples maps to kind sight", contradicting the schema's own `temple` enum value and the committed sample-japan output, which correctly uses `kind: "temple"` for actual temples. Fixed: `fx.currency` added to the schema (required) and to `web/lib/types.ts`, `ItineraryView.tsx` now interpolates it, `SKILL.md` step 6.2 now says to write it, both committed itinerary.json files patched with their currency. `review.md` and a new `SKILL.md` rule 6 now agree: `food` for edible, `temple` only for an actual temple/shrine, `sight` for every other non-food attraction regardless of destination, `transit`/`free` unchanged | Trust the first-pass fix as complete, run the independent QA verification the process calls for | The first pass fixed the two blockers Aman pointed at (rail tool, budget currency) but did not sweep the web layer or check that generalized prompt wording actually agreed with the schema and the one real committed example. An independent reviewer with no stake in the first pass caught both in one pass | None, schema/type/component/prompt text, two already-committed itinerary.json files patched for the schema's new required field | `docs/contracts/itinerary.schema.json`, `web/lib/types.ts`, `web/components/ItineraryView.tsx`, `.claude/skills/plan-trip/SKILL.md`, `.claude/agents/review.md`, `trips/sample-japan/itinerary.json` |
| D-030 | 20-09-2026 | Dynamic-destination audit and fix, triggered by Aman asking why the system checks "Japan only" after seeing the France run and asking for a full sweep. Real blocker found: `get_rail_route` never attempted a live call for any non-Japan route, it only checked the Japan seed file, so every other country's inter-city leg was permanently could-not-verify even though a live probe confirmed Google Routes TRANSIT genuinely works outside Japan (Paris to Lyon returned a real SNCF fare and duration). Fixed to check seed first, then fall through to live TRANSIT. Second real blocker: `budget.md` rule 1 hardcoded `convert_currency(1, "USD", "JPY")`, forcing every trip's local currency to JPY regardless of destination, fixed to determine the ISO 4217 code from the brief. Non-blocking generalizations: destination-research's like-query examples, review's like-to-kind mapping, logistics' inter-city table header and "temples/restaurants" wording, the itinerary template's "temple entries" budget line, all only Japan-flavored wording, not functional blocks, but capable of biasing behavior on a new destination, generalized. `itinerary.schema.json`'s `intercity[].duration_min` widened to allow `null`, matching `fare_usd`, since it previously could not represent an honest could-not-verify inter-city leg | Leave `get_rail_route` Japan-only and rely on agents phrasing around it, fix the tool | The France run exposed this by accident (I had to write "could not verify" for a route Google could actually answer). A tool that silently gives up rather than trying is worse than one that tries and fails honestly | None, prompts, one tool file, one schema field | `.claude/agents/{budget,logistics,destination-research,review}.md`, `.claude/skills/plan-trip/{SKILL.md,itinerary-template.md}`, `mcp/travel-tools/travel_tools/rail.py`, `mcp/travel-tools/server.py`, `docs/contracts/itinerary.schema.json`, spec section 4/5, `CLAUDE.md` |
| D-029 | 20-09-2026 | Two infrastructure gaps fixed after the first non-Japan run (France, Paris-Lyon): `scripts/worker_eval.py`'s `budget.fx` regex was hardcoded to `JPY`, generalized to any 3-letter currency code. `.claude/agents/logistics.md` rule 7 already bans semicolons, but the France run's logistics output still had one ("...fare; confirm directly..."), hand-fixed in the artifact, no prompt change since the rule already exists and this was a single miss, not a pattern | Leave the JPY-only regex since only Japan is demoed, generalize now | Both were never tested against a second geography before this run. The eval bug would have silently reported "FX line: missing" as a fail-worthy finding for every non-Japan trip forever. The semicolon is one miss, not evidence the rule needs strengthening | None, eval script and one artifact string only | `scripts/worker_eval.py`, `trips/france-paris-lyon-0000/02-logistics.md` |
| D-028 | 20-09-2026 | Review agent moved from Opus to Sonnet. Re-run of `scripts/review_eval.sh` on Sonnet: 4 of 5 fixtures clean, `six-days` mismatches on `travel_time_realistic` (expected pass, Sonnet fails it). Accepted as a known residual, not reverted, because the France dry run the same day showed Sonnet review correctly catching two real defects (an undercounted food-slot total, a mis-resolved Paris venue producing a 466 km "walking" leg) that an Opus review would also have needed to catch, so the swap is net positive even with one fixture flake | Leave review on Opus, move it to Sonnet, revert after the fixture mismatch | Aman's instruction. Review's six checks are structural, not open judgement (day count, city coverage, budget arithmetic, crowd-tactic keyword presence, travel-time thresholds), the same reasoning D-019 already applied to the orchestrator. The one flaking fixture needs its own investigation (add a `six-days` regression case to `scripts/tests/`), tracked as follow-up, not a reason to revert | Slightly less careful prose in `failures[].reason`, one review fixture (`six-days`, `travel_time_realistic`) needs investigation before the fixture suite can be trusted as a hard gate again | `.claude/agents/review.md`, spec section 4, `docs/tech-stack.md`, `CLAUDE.md`, `docs/architecture.md`, `docs/agent-flow-diagram.md` |
| D-027 | 19-09-2026 | Final whole-branch review: amend spec section 10 criterion 3 to the measured run time (was "under 4 minutes", predates the Sonnet orchestrator swap), and record three deviations already true of the shipped code that the spec text had not caught up with: the section 7 repo layout tree, the section 4 review agent's tools column, and the section 5 cache TTL and empty-result caching behaviour | Leave the spec stale and let the mismatch stand, or amend the spec text to match the code and measured behaviour in the same commit as the fix wave | The spec is binding per `CLAUDE.md`, so a spec that no longer matches the shipped code is a defect the next session inherits silently. All four items were true of the code before this commit, only the spec text was behind | None, spec and changelog text only | `docs/superpowers/specs/19-09-2026-travel-planner-multi-agent-design.md` sections 4, 5, 7, 10, `docs/superpowers/specs/changelog.md` |
| D-026 | 19-09-2026 | Two Task 16 review findings fixed at the source, not in the artifact. (1) Semicolons found in the hand-copied `trips/sample-japan/itinerary.md` were hand-fixed in that file, not in the rule the writers follow, so a ban on semicolons is added to `.claude/skills/plan-trip/SKILL.md` Hard rules and to the "No em dashes" rule in `.claude/agents/logistics.md`, `destination-research.md`, `budget.md`. (2) The `six-days` review fixture's `## Day 6: Kyoto, extra day (not in brief)` heading invited the review model to read it as a note rather than a day, flaking the `days_fit` check on one run in five; the heading is reworded to `## Day 6: Kyoto, Arashiyama` and `.claude/agents/review.md` check 1 is made explicit that every `## Day N` heading counts regardless of its text | Patch the artifact/fixture directly, fix the upstream rule or prompt that produced it | A patched artifact or fixture regenerates the defect on the next run since nothing changed in what produces it. The rule and the fixture heading are the actual causes | None, prompt and fixture text only | `.claude/skills/plan-trip/SKILL.md`, `.claude/agents/logistics.md`, `.claude/agents/destination-research.md`, `.claude/agents/budget.md`, `.claude/agents/review.md`, `tests/review-fixtures/six-days/04-itinerary-draft.md` |
| D-025 | 19-09-2026 | Three prompt fixes from the two real runs before the sample run: slug rule no longer asks for a random hex the orchestrator cannot generate reliably (was retrying for minutes), destination-research caps at 10 rows and 4 must-do per city (Kyoto returned 14 candidates and 6 must-do), synthesis Step 3 rule 1 requires every morning/afternoon/evening slot to name a venue from 01-destinations.md with `free` only allowed on the departure day's last slot (run 2 left two evening slots unscheduled) | Leave as-is and accept the recurring cost, fix the three prompt rules | Each defect was observed twice across the two dry runs and is cheap to close at the prompt level before the graded sample run | None, prompts only | `.claude/skills/plan-trip/SKILL.md` Step 1 and Step 3 rule 1, `.claude/agents/destination-research.md` rule 2 |
| D-024 | 19-09-2026 | Add a mechanical structural eval over `trips/<slug>/` run folders, covering the three workers and the orchestrator | LLM-judge eval, mechanical eval | Structure is what can be checked deterministically, and it is where the last three review findings lived | Says nothing about quality | `scripts/worker_eval.py`, Task 16 |
| D-023 | 19-09-2026 | v1 stays on the Claude Code runtime. A standalone server with no Claude Code dependency (any provider with function calling, Gemini included) is Phase 2, after Tasks 12 to 16 | Switch now | The workshop demo is the Claude Code machinery itself. About 70% of v1 carries into Phase 2 (tools, contracts, prompts, fixtures, UI) | Provider lock-in until Phase 2 | `docs/standalone-server-handover.md` |
| D-022 | 19-09-2026 | `scripts/review_eval.sh` scopes the driving `claude -p` call to `--allowedTools "Agent,Read,Edit(trips/**)"` instead of `--allowedTools "Agent,Read,Write"` | Leave unscoped (`Write`), scope to `Edit(trips/**)` | The unscoped driver mutated `.claude/agents/review.md` on disk during the first eval run (changed `model: opus` to `model: sonnet`, caught and reverted before commit). Per the official permissions doc, path rules are consulted only as `Edit(path)`, a `Write(path)` rule is accepted but never checked, and `Edit` rules govern `Write` too. The review subagent only ever writes `trips/fixture-<name>/05-review.json`, which `Edit(trips/**)` covers | None | `scripts/review_eval.sh` |
| D-021 | 19-09-2026 | Web path authenticates with the local Claude Code login first (local, single-user demo), `ANTHROPIC_API_KEY` second, Claude on Google Cloud (`CLAUDE_CODE_USE_VERTEX=1`) if it is ever offered to others. A Gemini API key is not an option, local or cloud, because Claude Code only calls Claude models. Task 13 verifies the no-key path | Gemini key, claude.ai login for the web app | The Agent SDK runs Claude Code, which only calls Claude models, via Claude Platform, Bedrock, Google Cloud Agent Platform or Foundry ([quickstart](https://code.claude.com/docs/en/agent-sdk/quickstart)). Anthropic does not allow third-party products to offer claude.ai login without approval ([overview](https://code.claude.com/docs/en/agent-sdk/overview)). Gemini would mean rebuilding orchestration outside Claude Code and losing the readable agent files | Pay per token for the web path, or a Google Cloud project for Vertex | Task 13 route, `.env.example`, `docs/tech-stack.md` section 4 |
| D-020 | 19-09-2026 | `budget.fx.rate` in `itinerary.schema.json` allows `null` alongside `number` | Omit the rate key when FX could not verify, allow null | The UI needs a stable shape, a key that is sometimes present and sometimes missing is harder to render than one that is always present with a nullable value | None | `docs/contracts/itinerary.schema.json`, `.claude/skills/plan-trip/SKILL.md` Step 6 |
| D-019 | 19-09-2026 | Orchestrator runs on Sonnet (terminal: `claude --model sonnet`, web: `model: "claude-sonnet-5"` in the route). Review stays on Opus. Supersedes the orchestrator half of D-002 | Keep Opus everywhere | Dry run: $6.41 per run, $4.71 Opus. Synthesis against a template is mechanical, the gate is where judgement sits | Weaker synthesis prose, possible more repair loops, measured in Task 16 | Spec section 4, `CLAUDE.md`, tech-stack, architecture, plan Task 13 route options |
| D-016 | 19-09-2026 | Remove the `env` object (`GOOGLE_MAPS_API_KEY`, `GOOGLE_MAPS_API` placeholders) from `.mcp.json` | Keep the placeholders, remove them | Redundant after D-015, the server now loads the repo `.env` itself, and the placeholders make `claude mcp list` print a "Missing environment variables" warning | None | `.mcp.json` |
| D-015 | 19-09-2026 | `server.py` loads `.env` from the repo root itself, before the key check, using `os.environ.setdefault` so an exported variable still wins | Export the key in the shell before every run | Claude Code does not load the repo `.env` when it launches an MCP server from `.mcp.json`, so `claude mcp list` would fail unless the key is exported in the shell first | A stale `.env` value wins over nothing but never over an exported variable | `mcp/travel-tools/server.py`, `mcp/travel-tools/tests/test_server.py` (`TRAVEL_TOOLS_DOTENV` override) |
| D-014 | 19-09-2026 | `.env` was committed in `b73cca6` and `a2e7a82` (controller `git add -A` before `.gitignore` existed). Untracked in the next commit. History purge scheduled as the last step before any push, key rotation if pushed before the purge | Purge immediately | A history rewrite while implementer agents commit on top loses their work (L-028). No remote exists, so the exposure is local only | Purged from every ref before the first push on 19-09-2026, verified with git log --all -- .env empty | ledger, this row, pre-push checklist |
| D-013 | 19-09-2026 | UI components are built under the `frontend-design:frontend-design` skill, MUI as the floor | Plain MUI defaults | Aman's instruction. A demo UI that looks like a template undercuts the product story (see L-010 in the global ledger: passing gates is not looking good) | Slightly more design effort in Tasks 14 and 15 | Plan Global Constraint 12, Task 14 and 15 dispatches |
| D-012 | 19-09-2026 | `CLAUDE.md` augmented beyond the plan's Task 1 text: architecture summary, full command table, process file index, rule 7 (feedback.md), rule 8 (commit format) | Keep the plan's minimal version | A fresh session needs the cross-file picture and the exact commands without reading six docs. The plan's rules are kept verbatim | Task 10 appends an Orchestration section, it must not rewrite this file | `CLAUDE.md`, plan Task 10 Step 3 unchanged |
| D-011 | 19-09-2026 | Keep a decision log and a spec changelog from task 1 onward, and make every subagent update them | Track only in git history, or only in the plan | Git shows what changed, not why. Subagents do not see this conversation, so the reason has to live in a file they read | One extra edit per decision | This file, `docs/superpowers/specs/changelog.md`, plan Global Constraint 11, `CLAUDE.md` Rules |
| D-010 | 19-09-2026 | Code reads `GOOGLE_MAPS_API_KEY` and falls back to `GOOGLE_MAPS_API` | Rename the user's `.env` key, or hardcode one name | The repo `.env` already carried `GOOGLE_MAPS_API`. Accepting both avoids a silent startup failure on rename | Two env names in circulation | `travel_tools/walking.py` `api_key()`, `.env.example` |
| D-009 | 19-09-2026 | Replace `get_transit_route` with `get_walking_route` (Routes WALK) plus `get_rail_route` (seeded JR Central data) | Keep the transit tool and let it fail, use a third-party Japan transit API (Navitime via RapidAPI), let agents estimate from memory | Live probe with the repo key: Routes transit returns `{}` for every request inside Japan while London works with the identical request. WALK works in Tokyo. JR Central publishes the fare table officially (Tokyo to Kyoto Nozomi reserved ¥13,970). Third-party APIs break the "official sources only" rule | No metro or bus timings for Japan, agents say "could not verify" | Spec section 5 amendment, plan Task 6, `docs/tech-stack.md`, `docs/architecture.md` |
| D-008 | 19-09-2026 | Budget agent produces price bands, the orchestrator does the sum | Run Budget after Logistics so it can price real hotels | The problem statement says the three workers run in parallel. Budget cannot price hotels it has not seen, so bands keep the fan-out honest without a hidden sequential dependency | The total is a midpoint estimate, review recomputes it | Spec section 3, plan Task 9 budget agent and Task 10 Step 3 |
| D-007 | 19-09-2026 | Review runs at most one repair loop, then the plan ships with warnings | Report only with no loop, loop until pass up to 3 times | Shows the evaluator-optimizer pattern without unbounded cost or demo time | A failing plan can still ship, clearly marked | Spec section 2, plan Task 10 Step 5 |
| D-006 | 19-09-2026 | Review agent reads only the brief and the draft, has no MCP servers and cannot edit the plan | Give review the worker reports and tools so it can fix things | Independence by construction. A reviewer that can fix is a second builder, and a reviewer that sees the builder's rationale inherits its blind spots | Review cannot check a fact against a tool, it checks structure and arithmetic | Plan Task 9 `review.md` |
| D-005 | 19-09-2026 | Hotels via Google Places names, areas, ratings and `price_level`, with nightly bands estimated by the Budget agent | Amadeus production with real rates (needs card), seeded hotel JSON | No free API gives real Tokyo rates without a card. Amadeus test tier has no Japan hotel coverage documented. Estimates labelled as estimates keep the product honest | Hotel prices are estimates, not quotes | Spec section 2, plan Task 9 logistics and budget agents |
| D-004 | 19-09-2026 | Next.js one-page front end with a live agent timeline, talking to the agents through the Agent SDK | Form plus final itinerary only, static HTML rendered from markdown, no UI | "Watch the agents work" is the point of the demo. The SDK loads the same `.claude/agents` the terminal uses, so one set of agent files serves both | Two runtimes (Node UI, Python MCP). Subagent output arrives per message, not per token | Spec section 6, plan Tasks 12 to 15 |
| D-003 | 19-09-2026 | Facts come from real APIs through a Python MCP server, not from web search | WebSearch in agents, local seed data, hybrid | Named tools with typed JSON make the handoffs visible and testable, and free tiers cover a demo many times over. Web search is non-deterministic and cannot be unit tested | One more process to run, one Google key to obtain | Spec section 5, plan Tasks 2 to 7 |
| D-002 | 19-09-2026 | Sonnet for the three workers, Opus for the orchestrator and the review | All Opus, all Sonnet | Workers do extraction and tool calling. Judgement sits at synthesis and at the gate | Slightly weaker prose in worker reports | Plan Task 9 frontmatter |
| D-001 | 19-09-2026 | Claude Code native runtime: orchestrator is the main session, workers are `.claude/agents/*.md` | Agents defined in TypeScript via the Agent SDK `agents` option, plain Messages API with hand-rolled orchestration | Agents are markdown a PM can read, the cohort has already met subagents, and the same files run from terminal and UI | Orchestrator logic lives in a skill prompt, not in code, so it is tested by running it | Spec section 2, plan Tasks 9 and 10 |
