# Spec Changelog

Every change to `19-09-2026-travel-planner-multi-agent-design.md` after its approval is logged here, newest first. The rule: if the build deviates from the spec, edit the spec and add a row here in the same commit. A change in the spec without a row here, or a row here without the spec edit, is a defect.

Each row answers: when, which section, what changed, why, what triggered it, and the decision ID in `docs/decisions.md` if there is one.

| Date | Section | Change | Why | Trigger | Decision |
|---|---|---|---|---|---|
| 20-09-2026 | 10 Trip revision | Three-reviewer pass found and fixed 8 issues: non-terminal city extension date math, unvalidated target_city, cutoff intercity-leg removal on revisit itineraries, a semicolon, missing web timeline attribution for trip-revision, missing stays/intercity cleanup on shortening, route.ts duplication (extracted to web/lib/agent-stream.ts), spec subsection numbering collision | Independent code review after implementation | Aman: "fix all 8" | D-034 |
| 20-09-2026 | 10 Trip revision | Implemented per `docs/superpowers/plans/20-09-2026-trip-revision.md`: `days[].date` backfilled in `SKILL.md` Step 6.2 and `trips/sample-japan/`, `revision.schema.json` added, `trip-revision` classifier agent added, `/revise-trip` skill added, fixture-based `scripts/revision_eval.sh` added (3/3 fixtures pass), `web/app/api/revise/route.ts` and `ReviseForm.tsx` added, `web/vitest.config.ts` added (the `@/` path alias was never wired into vitest, a pre-existing gap the new route test exposed) | Plan execution | Plan execution | D-033 |
| 20-09-2026 | 10 Trip revision (new section) | Trip revision capability brainstormed and written into the spec: `/revise-trip` skill, new `trip-revision` classifier agent, `06-revisions.json` append-only log, `days[].date` backfill prerequisite | Aman: a finished plan should accept a date change, or the traveler ending the trip early or extending it mid-trip, without breaking the existing plan's link | Brainstorm session | D-032 |
| 20-09-2026 | 6 Web app (implied), 4 Agents | Independent QA on the dynamic-destination fix found the web UI still hardcoded "JPY" in the FX caption (schema's `fx` object had no currency field to render), and `review.md`'s rewritten like-mapping contradicted the schema's own `temple` kind value. `fx.currency` added to the schema and UI, `review.md` and `SKILL.md` now agree on kind semantics | The first fix pass covered the agent prompts and the MCP tool but not the web layer or cross-file consistency | Independent QA verification pass (per Aman's requested review-fix-QA loop) | D-031 |
| 20-09-2026 | 4 Agents, 5 MCP travel-tools | `get_rail_route` now falls through to a live Google Routes TRANSIT call for every non-Japan route instead of always returning could not verify. Budget agent determines the destination's local currency instead of hardcoding JPY. Destination-research, logistics and review's like-matching rule generalized away from Japan/temple-only wording. `intercity[].duration_min` allows `null` | Aman: "the agents in this particular program should not be specific to any city, any destination, or any parameter... it must be dynamic". Audit found `get_rail_route` never attempted a live call for non-Japan routes even though Google Routes TRANSIT genuinely works outside Japan (only Japan is broken), and the budget agent's currency was hardcoded to JPY | `/goal` audit for static/destination-specific parameters | D-030 |
| 20-09-2026 | 4 Agents | Review agent model changed from Opus to Sonnet | Aman's instruction. The six review checks are structural (day count, city coverage, budget arithmetic, keyword matching), review was the last agent still on Opus, and D-019 already established the orchestrator does not need it | Aman's instruction | D-028 |
| 19-09-2026 | 7 Repo layout | Layout tree corrected to `travel_tools/{places,walking,rail,currency,weather,cache}.py`, matching the code on disk | The tree listed a `tools/{places,routes,currency,weather}.py` shape that never existed, the real package is `travel_tools/` | Final whole-branch review 19-09-2026 | D-027 |
| 19-09-2026 | 4 Agents | Review row's tools column expanded to "Read, Write (Write is needed for 05-review.json, independence is by prompt and by the absence of MCP tools)" | The row said "Read only, no Write to the plan" but review has always written its own `05-review.json`, independence never depended on withholding Write | Final whole-branch review 19-09-2026 | D-027 |
| 19-09-2026 | 5 MCP travel-tools | Cache TTL amended to 24h for places, walking and currency, 6h for weather (forecasts change); an empty Places result stated as a valid, cacheable answer | The spec only said "24 hours" for every tool, but the shipped `weather.py` already used a 6h TTL, and the cache code never special-cased an empty result as uncacheable | Final whole-branch review 19-09-2026 | D-027 |
| 19-09-2026 | 10 Success criteria | Criterion 3 changed from "under 4 minutes" to "under 15 minutes with a Sonnet orchestrator (measured 11m49s and 13m27s on 19-09-2026, each with one repair round). Reducing run time is a follow-up" | The 4-minute figure predated the Opus-to-Sonnet orchestrator swap (D-019) and was never re-measured against a real run | Final whole-branch review 19-09-2026 | D-027 |
| 19-09-2026 | 9 Testing | Worker and orchestrator eval added | Nothing evaluated the three workers or the orchestrator, only the review agent | Aman asked whether evals existed after the first dry run | D-024 |
| 19-09-2026 | 4 Agents | Orchestrator model changed from Opus to Sonnet. Review stays Opus | First dry run cost $6.41, $4.71 of it Opus. The orchestrator merges worker files against a template, which does not need Opus | Aman's instruction after reading the dry-run results page | D-019 |
| 19-09-2026 | 5 MCP travel-tools, 3 Architecture, 4 Agents, 10 Success criteria | `get_transit_route` (Google Routes TRANSIT) replaced by `get_walking_route` (Routes WALK) and `get_rail_route` (seeded from the official JR Central smartEX fare table). Logistics agent tool list and inter-city output updated. Success criterion 1 now traces the Tokyo to Kyoto leg to `get_rail_route` | Routes API returns no transit routes inside Japan. Control test: London transit works with the identical request, Tokyo to Kyoto, Tokyo intra-city and lat/lng requests return `{}`. WALK mode works in Tokyo (Senso-ji to Ueno Park, 1,911 m, 29 min) | Live probe with the repo key during plan writing, at the user's request to check API access | D-009 |
| 19-09-2026 | 5 MCP travel-tools, 7 Repo layout (implied) | Environment variable: code accepts `GOOGLE_MAPS_API_KEY` and falls back to `GOOGLE_MAPS_API` | The repo `.env` already used the shorter name | Found while probing the key | D-010 |
| 19-09-2026 | (process, no spec section) | This changelog and `docs/decisions.md` created. Plan Global Constraint 11 and `CLAUDE.md` now require both to be updated with every deviation | Subagents executing the plan do not see the conversation where decisions were made, so the reason must live in a file | User instruction before execution started | D-011 |
| 19-09-2026 | all | Spec approved after four-section review in brainstorm | Baseline | Brainstorm session | D-001 to D-008 |
