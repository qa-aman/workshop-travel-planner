[project]
name = "travel-tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["mcp>=2,<3", "httpx>=0.27"]

[dependency-groups]
dev = ["pytest>=8", "respx>=0.21"]

[tool.pytest.ini_options]
testpaths = ["tests"]
